# Copy to .env.local and fill in your SMTP credentials
# Example for Gmail (App Password) or any SMTP provider
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password
EMAIL_TO=hormisuelo@hotmail.com

# Optional: Formspree endpoint. If set, front-end will post to Formspree instead of using backend.
NEXT_PUBLIC_FORMSPREE_ENDPOINT=
